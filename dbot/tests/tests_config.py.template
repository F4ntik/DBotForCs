# Test Configuration Template
#
# This file provides a template for configuring test-specific parameters.
# To use it:
# 1. Copy this file to `tests_config.py` in the same directory (`dbot/tests/`).
# 2. Edit `tests_config.py` with your actual local environment details.
#
# `tests_config.py` is ignored by git (ensure it's in .gitignore)
# to prevent committing your personal credentials.

# MySQL Database Configuration (for test_mysql.py)
MYSQL_HOST = "127.0.0.1"
MYSQL_PORT = 3306
MYSQL_USER = "your_mysql_user"  # Replace with your MySQL username
MYSQL_PASSWORD = "your_mysql_password"  # Replace with your MySQL password
MYSQL_DB = "mysql_test"  # The test database name

# RCON Configuration (for test_rcon.py)
RCON_HOST = "127.0.0.1"
RCON_PORT = 27015  # Default CS RCON port
RCON_PASSWORD = "your_rcon_password"  # Replace with your RCON password

# Web Server Configuration (for test_webserver.py)
WEB_SERVER_HOST = "127.0.0.1"
WEB_SERVER_PORT = 8080  # Default port used in web_server tests
# ALLOWED_IPS should be a list of strings, e.g., ["127.0.0.1", "::1"]
ALLOWED_IPS = ["127.0.0.1"]

# Add any other test-specific configurations below as needed.
